
곱집합

관계(Relation)
순서 관계
$$
2,3 in \mathbb{N}
2 <= 3 => (2,3) in R

$$

동치 관계
$$
1 -= 4(mod3)
1-4 = -3 " 3의 배수 " 
$$
모듈로(법)3으로 1과 4가 합동(동치) 다.
$$
ㅇ 
$$

정수 집합은 무한하다.
3으로 나눈 나머지가 같다라는 동치 관계로 분류하면 
3가지로 유한하다.

집합의 곱셈이 있으면 무슨 연산도 있을까요? 나눗셈 
곱집합(product set) <-> 몫집합(quotient set)

곱집합의 원소: 순서쌍(x,y)
몫집합의 원소는? 동치류 (equivalence class) 
e.g.) bar0, bar1, bar2 

$$
\mathbb{N}
$$

자연수를 한마디로 정의하면? 함수로 정의되어 있다.

### 벡터공간

